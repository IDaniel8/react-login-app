**A basic example of login with token and react-persist**

## Getting Started

To get you started, just `$ npm install` and then `$ npm start`.
Test the login with `prueba@gmail.com` and password `123`
